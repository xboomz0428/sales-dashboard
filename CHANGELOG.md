# 更新日誌 CHANGELOG

## v0.0.86（2026-07-07）

- **LC 自動匯入改為在 ERP 主機執行**（因 MariaDB 僅開放本機連線）：`lc-inspect.mjs` 強化為一次輸出「表清單＋候選銷貨表欄位＋樣本資料」，`LC_DB_HOST` 預設 localhost；新增 `.env.example` 與 `scripts/README-LC自動匯入.md`（ERP 主機安裝／探查／排程步驟）。待 schema 貼回後即可產出 `lc-import.mjs`。

## v0.0.85（2026-07-07）

- **產品成本匯入**：新增 `scripts/import-costs.mjs`，從 LC「商品資料.xls」把成本補進 user_costs（精確＋正規化比對、只補缺不覆蓋、支援 --dry-run/--overwrite）。同時直接補上 2angels 停售系列、mamayo 磁力片等高價值缺口，成本列覆蓋率 90.6% → 96.6%。
- **LC 直連準備**：新增 `scripts/lc-inspect.mjs`（唯讀探查 lcdata 結構）與 `mysql2` 相依，供後續撰寫 `lc-import.mjs` 自動匯入 ERP 銷貨資料。

## v0.0.84（2026-07-07）

- **AI 報告 prompt 強化**：加入公司背景（威斯邁母嬰多品牌 × 三型態通路）讓建議貼合實況；嵌入產品框架（BCG/JTBD/Kano/Ansoff/耗材回購/彌月）與通路框架（通路經濟學/RFM 分級/價格帶治理/新通路評估）；新增五位專家中的「通路發展顧問」；要求每個建議附「數據→動作→負責→時程→KPI→預期效果」可落地結構；加入好漢草/草本合規紅線。
- **新增 AI 分析類型「🧭 通路開發維護」**：經銷分級(RFM)、通路衝突與價格帶、新通路評估矩陣、90 天通路行動計畫。
- **新增落地手冊** `docs/策略框架_產品與通路開發.md`：產品與通路開發/維護的理論→落地→KPI，含 90 天啟動清單。

## v0.0.83（2026-07-07）

### 效能：資料庫載入大幅加速
- **本地快取（IndexedDB）**：首次載入後資料存在瀏覽器，之後開頁**先秒開快取**、背景比對資料庫戳記（筆數＋最大 id），有變動才重新抓取；上傳/備份還原會自動作廢快取。離線或連線失敗時退回快取並明確提示。
- **壓縮傳輸（get_sales_compact RPC）**：改用陣列格式回傳（欄位名不再重複 14 萬次），且單一 jsonb 回傳不受每頁 1000 筆限制——請求數 142 → 8 個，每次 2 萬筆。RPC 失敗自動退回舊版分頁讀取。無需調整 Supabase 後台設定。

### 新功能
- **ERP 自動匯入**（`scripts/erp-import.mjs` ＋ `erp-import.bat`）：本機/排程直接讀 NAS 的 ERP xls 寫入資料庫，與前端相同解析邏輯與「插新→驗證→刪舊」安全順序，支援 `--dry-run` 與 `--replace-all`。搭配 Windows 工作排程器即可每日自動匯入，設定見 `scripts/README-ERP自動匯入.md`。
- **通路毛利對比**（通路分析頁）：依產品成本管理的成本，按通路類型/網路實體拆解營收、成本、毛利、毛利率，並附「成本覆蓋率」提示（覆蓋率 <50% 標黃，代表該通路多數商品未設成本）。僅具成本檢視權限的角色可見。
- **Demo 模式遮名稱**：Demo 模式除金額/數量 ×4 外，客戶/產品/品牌名稱改以「客戶001、產品001、品牌001」等穩定假名顯示，展示與截圖不再露出真實名稱。

## v0.0.82（2026-07-07）

- 將「選取期間 vs 去年同期（同比）」摘要卡**同時套用到年對年、季對季、月對月**三個分頁（原本只有月對月）。抽成共用元件 `PeriodYoYCard`，行為一致。

## v0.0.81（2026-07-07）

- **對比分析 → 月對月**：新增「選取期間 vs 去年同期（同比）」摘要卡。顯示：
  - 本期間（目前篩選選取的總金額/數量）
  - 去年同期（對齊到去年同月份的總額）
  - 同比成長／衰退的**金額（$）與百分比（%）**，成長綠色、衰退紅色。
  隨「分析指標」自動切換金額/數量；若去年同期無資料（例如選到最早年份）會提示無法比較。單一產品篩選時暫不顯示此卡（避免與全域選取混淆）。

## v0.0.80（2026-07-07）

- **效能**：資料庫載入改為並行分頁（原本 140+ 次循序請求 → 6 路並行），大幅縮短登入到看見資料的時間。
- **總銷售金額顯示到小數點第 2 位**（例如 NT$ 1.24 億），其餘卡片格式不變。
- **新增 Demo 模式**：右上角可切換，開啟後所有金額與數量放大 4 倍以隱藏真實數據，供對外展示／截圖使用；按鈕會亮起「🎭 Demo ×4」提示目前非真實數據。（客戶／產品名稱不變，如需一併隱藏可再加。）
- 補上 `sales_data` 資料表的 migration 檔（`supabase/migrations/20260707000000_sales_data.sql`）進版控。

## v0.0.79（2026-07-07）

### 修正：年度銷售金額大幅短少（可能只剩實際的不到一成）

**問題根源（三個疊加）：**
1. 資料庫 `sales_data` 資料表從未被建立，程式讀/寫 DB 一律失敗，只能退回讀 Storage 的 `.rows.json` 快取。
2. 大型 `.xls` 檔在瀏覽器（尤其手機）解析時記憶體不足而中途失敗，只把「解析到一半的殘缺結果」存進快取，之後每次載入都讀這份殘缺資料。
3. `excelCore` 的檔案內去重把「同訂單、同商品、同數量、同金額」的**正當重複明細**誤判為重複而刪除。

**本次修正：**
- 建立 `sales_data` 資料表（含 `source_file`、`import_batch` 欄位與寫入權限政策），並以伺服器端解析一次灌入正確完整的歷史資料。DB 為主要資料來源，載入不再依賴瀏覽器解析。
- `excelCore.js`：移除會誤刪正當重複明細的去重；`_key` 加上檔案內行序號，確保每一筆銷售明細都保留（銷售金額為已發生事實，全部統計）。
- `useCloudData.js`：上傳改為**檔案層級冪等 + 寫入後驗證筆數**——先以新批次插入、驗證筆數相符後才刪除舊批次；插入失敗會還原並**明確報錯**，不再出現「✓ 已同步（DB 失敗）」這種假成功。
- `useCloudData.js`：DB 分頁讀取加入穩定的次要排序（date, id），避免同日期資料列在分頁時被跳過或重複。
- `App.jsx`：上傳時傳整份檔案的資料列給資料庫（而非去重後的差集），確保檔案層級取代不遺漏任何一筆。

### 其他
- 統一版本號顯示（登入頁 v0.0.24 → v0.0.79，與 package.json 同步），新增本 CHANGELOG。

> 備註：客戶狀態、產品狀態欄位不影響金額統計——程式從未讀取或以其過濾，所有停止/停售/臨時/空白狀態的資料列皆完整計入。
